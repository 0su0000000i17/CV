export function createStrictRetryNotice(previousError: string) {
  return `
Предыдущий ответ был отклонён backend-парсером.

Причина:
${previousError}

Верни результат заново, строго следуя схеме и требованиям из инструкций выше.

ЖЁСТКИЕ ПРАВИЛА:
- Ответ должен быть только одним валидным JSON-объектом.
- Не используй markdown и не используй \`\`\`json.
- Не добавляй текст до или после JSON.
- Все ключи и строковые значения должны быть в двойных кавычках, экранируй кавычки внутри строк.
- Не используй trailing comma.
`.trim();
}
